export interface CardViewOptions {
  showDescriptions: boolean;
  showLabels: boolean;
  useLocalDateFormat: boolean;
  numberLists: boolean;
  numberCards: boolean;
  showDates: boolean;
  showTitles: boolean;
  newPagePerList: boolean;
}
